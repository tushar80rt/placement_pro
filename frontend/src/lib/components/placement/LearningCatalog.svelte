<script lang="ts">
  import type { LearningCategory } from '$lib/data/types'; import DifficultyBadge from './DifficultyBadge.svelte';
  let { title, subtitle, categories, basePath }: { title: string; subtitle: string; categories: LearningCategory[]; basePath: string } = $props(); let query = '';
  let shown = $derived(categories.filter(c => c.title.toLowerCase().includes(query.toLowerCase())));
</script>
<div class="mx-auto max-w-7xl px-4 py-8 md:px-8"><section class="rounded-[2rem] bg-[#17202b] p-8 text-white md:p-10"><p class="text-xs font-bold uppercase tracking-[.18em] text-orange-200">Practice library</p><h1 class="mt-3 text-4xl font-extrabold">{title}</h1><p class="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{subtitle}</p><input bind:value={query} class="mt-7 w-full max-w-xl rounded-2xl bg-white px-5 py-3 text-sm text-text-primary outline-none" placeholder="Search a topic..."/></section><section class="mt-9 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{#each shown as category}<a href="{basePath}/{category.slug}" class="group rounded-3xl border border-border/70 bg-white p-6 shadow-card transition hover:-translate-y-1 hover:shadow-card-hover"><span class="text-2xl text-primary">{category.icon}</span><h2 class="mt-6 font-extrabold">{category.title}</h2><p class="mt-2 text-sm text-text-secondary">Theory, formulas, solved examples and {category.questions.length} guided questions.</p><div class="mt-5 flex gap-2">{#each category.questions.slice(0, 2) as question}<DifficultyBadge difficulty={question.difficulty}/>{/each}</div></a>{/each}</section></div>
