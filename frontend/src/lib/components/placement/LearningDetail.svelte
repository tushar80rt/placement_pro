<script lang="ts">
  import type { LearningCategory } from '$lib/data/types'; import DifficultyBadge from './DifficultyBadge.svelte';
  let { category, backPath }: { category: LearningCategory; backPath: string } = $props(); let revealed: number | null = null;
</script>
<div class="mx-auto max-w-5xl px-4 py-8 md:px-8"><a href={backPath} class="text-sm font-bold text-text-secondary hover:text-primary">← All topics</a><header class="mt-6 rounded-3xl bg-[#17202b] p-8 text-white"><p class="text-xs font-bold uppercase tracking-[.18em] text-orange-200">Learn · practice · review</p><h1 class="mt-3 text-3xl font-extrabold">{category.title}</h1><p class="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{category.theory}</p></header><div class="mt-7 grid gap-4 md:grid-cols-2"><section class="rounded-2xl border border-border bg-white p-6 shadow-card"><h2 class="font-extrabold">Core concepts</h2><ul class="mt-4 space-y-3 text-sm text-text-secondary">{#each category.concepts as concept}<li>• {concept}</li>{/each}</ul></section><section class="rounded-2xl border border-border bg-white p-6 shadow-card"><h2 class="font-extrabold">Formula sheet</h2><ul class="mt-4 space-y-3 text-sm text-text-secondary">{#each category.formulas as formula}<li>• {formula}</li>{/each}</ul></section></div><section class="mt-7"><h2 class="text-xl font-extrabold">Practice questions</h2><div class="mt-4 space-y-3">{#each category.questions as question}<article class="rounded-2xl border border-border bg-white p-5 shadow-card"><div class="flex justify-between gap-3"><h3 class="font-bold">{question.id}. {question.question}</h3><DifficultyBadge difficulty={question.difficulty}/></div><div class="mt-4 grid gap-2 sm:grid-cols-2">{#each question.options as option, i}<button onclick={() => revealed = question.id} class="rounded-xl border border-border p-3 text-left text-sm transition hover:border-primary">{String.fromCharCode(65+i)}. {option}</button>{/each}</div>{#if revealed === question.id}<p class="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-800"><b>Solution:</b> {question.explanation}</p>{/if}</article>{/each}</div></section></div>
